configfile: 'config/6.3_config.yaml'

rule all:
    input:
        expand('results/6.3_{letter}.txt', letter=config['samples'])

rule samples:
    output:
        'results/6.3_{letter}.txt'
    shell:
        'echo {output} > {output}'