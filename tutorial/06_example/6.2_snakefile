samples = ['a', 'b', 'c']

rule all:
    input:
        expand('results/6.2_{letter}.txt', letter=samples)

rule samples:
    output:
        'results/6.2_{letter}.txt'
    shell:
        'echo {output} > {output}'