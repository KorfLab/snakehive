rule run_multi_out:
    output:
        'results/run_multi_out.txt',
        'results/1.5_ex.txt'
    run:
        with open(output[0], 'w') as out:
            out.write(f'These are the outputs: {output}')
        open(output[1], 'a').close()