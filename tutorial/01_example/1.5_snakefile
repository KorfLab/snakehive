rule typing:
    output:
        'results/typing.txt'
    run:
        with open(output[0], 'w') as out:
            out.write(f'This the typing of `output`: {type(output)}\n')
            out.write(f'\nThis the typing of `output[0]`: {type(output[0])}')