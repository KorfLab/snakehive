configfile: 'config/7.6_config.yaml'

rule all:
    input:
        'results/7.6_counts.txt'

module counting:
    snakefile:
        'rules/count.smk'
    config:
        config

use rule * from counting